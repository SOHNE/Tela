/* --- TelaBrew: visible focus outline for D-pad navigation --- */
(function () {
  var style = document.createElement('style');
  style.textContent = "__TELABREW_CSS__";
  (document.head || document.documentElement).appendChild(style);
})();
